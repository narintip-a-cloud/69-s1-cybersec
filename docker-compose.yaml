services:
  db:
    image: postgres:16-alpine
    container_name: 69-s1-db
    ports:
      
"54327:5432"
  environment:
    POSTGRES_PASSWORD: narin0910532620
    POSTGRES_USER: narintip
    POSTGRES_DB: narintip
  volumes:
./data:/var/lib/postgresql/data
